<?phpclass A{public function l($d,$s){if(s(d,'')==){returns(}e("");}d n t(){$s();  $call = array($class, 'noSuchMethod');
        
        if (is_callable($call)) {
            call_user_func($call);
        }
    }
}

class B extends A {